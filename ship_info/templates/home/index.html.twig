{% extends 'base.html.twig' %}

{% block title %}鹿児島〜沖縄・奄美大島フェリー運航情報{% endblock %}
{% block title_suffix %}{% endblock %}
{% block description_text %}Aライン・マリックスラインの鹿児島〜那覇・奄美大島間フェリーの最新運航状況。欠航・遅延情報を毎時更新。旅行前に出発港・到着港の運航状況をご確認ください。{% endblock %}

{% block body %}
<main>
    <section id="home">
        <h2>現在の運航状況（Aライン・マリックスライン）</h2>
        {% if ship_data is not empty %}
            {% for ship in ship_data %}
            <div class="ferry-status">
                <h3>
                    {% if ship.url %}
                        <a href="{{ ship.url }}" target="_blank" rel="noopener noreferrer">{{ ship.name }}</a>
                    {% else %}
                        {{ ship.name }}
                    {% endif %}
                </h3>
                <ul>
                {% for route in ship.routes %}
                <li>{{ route.name }}:
                    {% for s in route.statuses %}
                        <span class="status {{ s.class ?: 'normal' }}">{{ s.text }}</span>
                    {% endfor %}
                </li>
                {% endfor %}
                </ul>
            </div>
            {% endfor %}
        {% else %}
            <p>現在、運航情報はありません。</p>
        {% endif %}
    </section>
</main>
{% endblock %}
